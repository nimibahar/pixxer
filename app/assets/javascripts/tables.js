// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
$('.tabsts').each(function(){
   $("tr:contains('Hosted')").css('background-color','#ffff99');
   $("tr:contains('Full')").css('background-color','#ccffcc');
  // if($('.tabsts').text() == 'Hosted'){
  //   console.log("reached")
  //   $(this).
});

